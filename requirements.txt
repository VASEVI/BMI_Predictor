# 1. Create requirements.txt
Flask==2.3.4
pandas==2.1.1
numpy==1.26.2
joblib==1.3.2
scikit-learn==1.3.2

#2. Commands to set up the environment

# a) Create a virtual environment:
# On Windows
python -m venv venv

# On macOS/Linux
python3 -m venv venv

# b) Activate the virtual environment:

# Windows
venv\Scripts\activate

# macOS/Linux
source venv/bin/activate

# c) Upgrade pip (optional but recommended):
pip install --upgrade pip

# d) Install dependencies:
pip install -r requirements.txt
